name: Build Actual Usage Paper

on:
  push:
    branches: [ main, journal-actualusg ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker.io/texlive/texlive:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Versions
        run: |
          latex --version
          latexmk --version

      - name: Generate PDF
        run: |
          latexmk -pdf -pdflatex="pdflatex -interaction=nonstopmode -halt-on-error" paper-actualusage.tex

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-actualusage-pdf
          path: paper-actualusage.pdf
