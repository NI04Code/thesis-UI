name: Build Thesis

on:
  push:
    branches: [ journal-netaware ]
  pull_request:
    branches: [ journal-netaware ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker.io/texlive/texlive:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Versions
        run: |
          latex --version
          latexmk --version

      - name: Generate PDF
        run: |
          latexmk -pdf -pdflatex="pdflatex -interaction=nonstopmode -halt-on-error" paper-netaware.tex

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-netaware-pdf
          path: paper-netaware.pdf