name: Build Thesis

on:
  push:
    branches: [ journal-resourcedefrag ]
  pull_request:
    branches: [ journal-resourcedefrag ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker.io/texlive/texlive:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Versions
        run: |
          latex --version
          latexmk --version

      - name: Generate PDF
        run: |
          latexmk -pdf -pdflatex="pdflatex -interaction=nonstopmode -halt-on-error" paper-resourcedefrag.tex

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-resourcedefrag-pdf
          path: paper-resourcedefrag.pdf