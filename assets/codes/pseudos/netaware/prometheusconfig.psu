prometheusOperator:
  resources:
    limits: { memory: "200Mi" }
    requests: { cpu: "20m", memory: "100Mi" }

prometheus:
  prometheusSpec:
    retention: "6h"
    scrapeInterval: "45s"
    resources:
      limits: { memory: "450Mi" }
      requests: { cpu: "50m", memory: "200Mi" }