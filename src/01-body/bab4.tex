%-----------------------------------------------------------------------------%
\chapter{\babEmpat}
\label{bab:4}
%-----------------------------------------------------------------------------%

This chapter presents the evaluation of the Resource Defragmentation policy. The methodology in Chapter~\ref{bab:3} defines how the policy detects fragmentation, ranks candidate Pods with TOPSIS, applies a feasibility guard, and supports request-based, actual-usage, and EWMA-smoothed signals. The evaluation here proceeds in two stages: a synthetic cluster walk-through that traces the algorithm end-to-end on a small Kubernetes-shaped scenario, followed by a discrete-event simulator-based scale-out that compares the policy against established baselines on a larger heterogeneous cluster.

\section{Evaluation Approach}
\label{sec:evaluation_rationale}

The evaluation addresses two complementary questions. Following the directed Pod eviction perspective of \citet{larsson_klein_elmroth_2023}, descheduling must improve a fragmented Kubernetes cluster after the initial kube-scheduler placement; this is request-aware because Kubernetes scheduling feasibility is based on declared resource requests. Following load-aware orchestration work by \citet{marchese2024loadaware,marchese2025enhancing}, runtime CPU and memory usage may also serve as an additional eviction-ranking signal when Pod requests do not reflect actual behavior.

Both threads share a constraint: actual usage does not replace Kubernetes scheduling feasibility. A Pod can only be admitted by the scheduler if its declared requests fit on a Node. Fragmentation outcomes are therefore validated using schedulability evidence, while runtime usage is treated as an additional ranking signal rather than as a replacement for the scheduler's request-based admission.

The synthetic walk-through (Section~\ref{sec:synthetic_walkthrough_validation}) traces the algorithm end-to-end on a small cluster with explicit numerical inputs, exposing the lifecycle from a fragmented initial state through eviction to scheduler placement of both the freed slot and the evicted Pods' replacements. The simulator-based scale-out (Section~\ref{sec:simulator_topsis_rdg_evaluation}) reproduces the Kubernetes scheduling pipeline on a larger heterogeneous cluster derived from a publicly available cluster trace, comparing the policy against established utilization-based and greedy descheduling baselines.

\section{RII, RRF, and Fragmentation Evidence}
\label{sec:rii_rrf_fragmentation_evidence}

The implementation uses the Resource Imbalance Index (RII) as a lightweight CPU-memory imbalance signal. RII is useful for identifying Nodes whose resource shape is skewed and for ranking candidate evictions with TOPSIS. However, RII is not treated as sufficient proof that fragmentation has been solved.

The Relative Resource Fragmentation (RRF) idea from \citet{mishra_bellur_2016} is conceptually closer to a direct fragmentation metric because it asks whether remaining capacity is usable for a target request. This research keeps RRF as evaluation context rather than replacing the implementation algorithm. The practical validation is therefore RRF-like: before descheduling, aggregate free resources must be sufficient for the target Pod while no individual Node can fit it; after descheduling, the target Pod should become schedulable or Running. The synthetic walk-through in Section~\ref{sec:synthetic_walkthrough_validation} concretizes this pattern with explicit numbers; the cluster has \(1800\,m\) of aggregate free CPU but no Node fits the \(900\,m\) probe, until two evictions free a single feasible slot.

Thus, RII and TOPSIS are used as decision signals, while the final claim is grounded in schedulability outcomes. This avoids claiming success only because an index improved.

\section{Synthetic Cluster Walk-Through}
\label{sec:synthetic_walkthrough_validation}

The first stage of the evaluation is a synthetic cluster walk-through whose every state can be derived analytically. The walk-through has two purposes: it traces the descheduler decisions on inputs that can be checked by hand, and it exposes the schedulability lifecycle of the probe Pod under a small range of \(MaxEvictions\) values together with the default-scheduler placement of the evicted Pods' replacements.

The scenario, RII, FSI, priority, and TOPSIS calculations are described in Section~\ref{sec:topsis_example}. The same script\footnote{Available in the custom descheduler repository under \texttt{experiments/synthetic-simulation/simulate.py}.} is reused here to extend the analysis. After each defragmentation pass, the script feeds the post-eviction Node states and the queue of replacement Pods plus the probe Pod to a default-kube-scheduler-style placement stage. That stage applies the \texttt{NodeResourcesFit} filter and a \texttt{LeastAllocated} score equal to the mean of the per-resource fractions \((C - (R + r))/C\), with lexicographic tiebreak between equally scored Nodes. Two scheduling orders are evaluated to expose order sensitivity: \emph{probe-first}, where the previously Pending probe is admitted before any replacement, and \emph{replacement-first}, where the ReplicaSet replacements are admitted before the probe.

Table~\ref{tab:synthetic_maxev_sweep} reports the result of sweeping \(MaxEvictions\) from \(1\) to \(5\) at the fixed threshold \(0.2\). The eviction list is the sequence emitted by the policy. The probe-first and replacement-first columns report the placement of the probe Pod under each scheduling order.

\begin{table}[htbp]
    \centering
    \caption{MaxEvictions Sweep on the Synthetic Cluster (Threshold \(=0.2\))}
    \label{tab:synthetic_maxev_sweep}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{c c l l l}
        \hline
        \textbf{\(MaxEv\)} & \textbf{Evictions} & \textbf{Eviction Sequence} & \textbf{Probe (probe-first)} & \textbf{Probe (replacement-first)} \\
        \hline
        \(1\) & \(1\) & \(M\text{-}1\) from \(B\)                             & Running on \(B\) & Running on \(B\) \\
        \(2\) & \(2\) & \(M\text{-}1/B\), \(C\text{-}1/A\)                    & Running on \(B\) & Running on \(B\) \\
        \(3\) & \(3\) & \(M\text{-}1/B\), \(C\text{-}1/A\), \(C\text{-}2/A\)  & Running on \(A\) & Running on \(B\) \\
        \(4\) & \(4\) & \(M\text{-}1/B\), \(C\text{-}1/A\), \(C\text{-}2/A\), \(C\text{-}3/A\) & Running on \(A\) & Running on \(B\) \\
        \(5\) & \(4\) & same as \(MaxEv = 4\)                                & Running on \(A\) & Running on \(B\) \\
        \hline
    \end{tabular}
    }
\end{table}

Three observations follow from this sweep. First, the row \(MaxEv = 1\) shows that a single eviction is sufficient to unblock the probe in this scenario, because the chosen Pod \(M\text{-}1\) frees exactly the \(100\,m\) of CPU that Node \(B\) needs to satisfy the \(900\,m\) probe. The descheduler does not have direct knowledge of the probe; the alignment between the TOPSIS choice and the probe's resource shape is a consequence of the imbalance-aware scoring rather than an explicit hint. Second, the row \(MaxEv = 5\) only produces four evictions, because Node \(A\) drops out of the fragmented list once the loop reaches \(RII = +0.15 \le \) threshold. \(MaxEvictions\) is therefore an upper bound, not a target. Third, the probe-first and replacement-first columns disagree from \(MaxEv = 3\) onward: when the budget overshoots, the default scheduler can place the probe on Node \(A\) if it is admitted before the replacements take the larger free-CPU slot, and on Node \(B\) otherwise. The probe still becomes Running in both orders, but the placement is not unique once the cluster has multiple feasible Nodes.

A practical consequence is that the descheduler outcome cannot be evaluated only by counting evictions. Even a budget that overshoots can be effective, but it also commits the cluster to broader churn whose final placement depends on the scheduler ordering. In this scenario the lowest-cost successful configuration is \(MaxEv = 1\); \(MaxEv = 2\) reaches the same schedulability outcome with one extra eviction; \(MaxEv \ge 3\) does not improve the binary success measure of the probe.

The scheduler stage also provides a sanity check on the failure mode that motivates the feasibility guard. In each successful row of Table~\ref{tab:synthetic_maxev_sweep}, the replacement Pods produced by the ReplicaSet (with the same request shape as the evicted Pods) are placed on Nodes other than the original origin under the \texttt{LeastAllocated} score. That outcome is favorable but not guaranteed in general; under a bin-packing score such as \texttt{MostAllocated}, replacements can return to the origin and erase the defragmentation effect. The walk-through therefore validates the policy under a default-style scheduler while documenting the dependency on the scheduler's scoring strategy as a known limitation.

\section{Simulator-Based Scale-Out}
\label{sec:simulator_topsis_rdg_evaluation}

The second stage of the evaluation scales the analysis to a larger cluster using a discrete-event Kubernetes simulator. This stage focuses on the request-based TOPSIS-RDG behavior under a real-world workload trace and compares it with common utilization-based descheduling strategies and the RDG baseline.

\subsection{Experimental Setup}
\label{sec:experimental_setup}

The simulator reproduces the Kubernetes scheduling pipeline, including filtering, scoring, reservation, and binding phases. Using a simulator allows the same workload trace and cluster topology to be repeated across policies without the noise introduced by live-cluster timing, autoscaling, or external workload variation.

\subsubsection{Workload: Google Borg Cluster Trace}
\label{sec:borg_workload}

The experimental workload is derived from the publicly available Google Borg cluster trace. Task submission events are converted into Pod-like workload objects and scaled against a reference machine of 96 vCPUs and 384 GiB memory.

To create multi-dimensional contention, Pods are classified into three strata through stratified sampling: \texttt{HIGH\_CPU}, \texttt{HIGH\_MEM}, and \texttt{BALANCED}. Resource requests are then scaled so that the total demand exceeds the simulated cluster capacity on both CPU and memory dimensions. This creates an initial set of unscheduled Pods that can be used to evaluate whether descheduling improves later schedulability.

\subsubsection{Cluster Topology}
\label{sec:cluster_topology}

The simulated cluster consists of 20 heterogeneous Nodes divided into three capacity tiers, as shown in Table~\ref{tab:cluster_topology}. The tiered topology is used to avoid an overly uniform bin-packing problem and to create realistic differences in Pod placement options.

\begin{table}[htbp]
    \centering
    \caption{Simulated Cluster Node Topology}
    \label{tab:cluster_topology}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{l c c c c}
        \hline
        \textbf{Tier} & \textbf{vCPUs (milliCPU)} & \textbf{Memory (MiB)} & \textbf{Weight} & \textbf{Expected Count} \\
        \hline
        Small & 16000 & 65536 & 30\% & 6 Nodes \\
        Medium & 32000 & 131072 & 40\% & 8 Nodes \\
        Large & 64000 & 262144 & 30\% & 6 Nodes \\
        \hline
    \end{tabular}
\end{table}

\subsubsection{Policies Under Evaluation}
\label{sec:policies_under_evaluation}

Five simulator configurations are evaluated:

\begin{itemize}
    \item \textbf{D1 -- No Descheduler.} Baseline control with no descheduling.
    \item \textbf{D2 -- LowNodeUtilization.} Official utilization policy that evicts Pods from underutilized Nodes to consolidate load.
    \item \textbf{D3 -- HighNodeUtilization.} Official utilization policy that evicts Pods from overutilized Nodes to spread load.
    \item \textbf{D4 -- ResourceDefragmentation.} The proposed TOPSIS-based multi-criteria eviction strategy.
    \item \textbf{D5 -- RDG.} The Resource Defragmentation Greedy strategy proposed by Guo et al.~\citep{guo2025joint}.
\end{itemize}

All descheduler-enabled runs use the same eviction budget so that policy effectiveness is not explained by allowing one policy to evict substantially more Pods than another.

\section{Results and Analysis}
\label{sec:results_analysis}

\subsection{Initial Scheduling}
\label{sec:initial_scheduling_results}

Table~\ref{tab:init_scheduling} shows the initial scheduling outcome before descheduling. Because the workload is intentionally oversubscribed, every policy starts with a comparable number of unscheduled Pods.

\begin{table}[htbp]
    \centering
    \caption{Initial Scheduling Phase Results}
    \label{tab:init_scheduling}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Experiment} & \textbf{Pods Scheduled} & \textbf{Pods Failed} & \textbf{CPU Alloc.} & \textbf{Memory Alloc.} \\
        \hline
        D1 & 4100 & 900 & 96.1\% & 96.0\% \\
        D2 & 4104 & 896 & 96.2\% & 96.3\% \\
        D3 & 4088 & 912 & 96.0\% & 96.2\% \\
        D4 & 4114 & 886 & 96.1\% & 96.4\% \\
        D5 & 4117 & 883 & 96.7\% & 96.1\% \\
        \hline
    \end{tabular}
\end{table}

The initial phase confirms that the workload creates similar pressure across all runs. Therefore, later differences in unscheduled Pod count, eviction count, and rescheduling yield can be attributed to descheduler behavior rather than to a substantially different initial workload.

\subsection{Descheduling Behavior}
\label{sec:descheduling_behavior}

The descheduling phase reveals the cost of each policy in terms of evictions and runtime, as shown in Table~\ref{tab:descheduling_behavior}.

\begin{table}[htbp]
    \centering
    \caption{Descheduling Phase Behavior}
    \label{tab:descheduling_behavior}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l l r r l}
        \hline
        \textbf{Exp.} & \textbf{Policy} & \textbf{Evictions} & \textbf{Time} & \textbf{Observation} \\
        \hline
        D1 & None & 0 & -- & No descheduling performed \\
        D2 & LowNodeUtilization & 0 & 0.14s & No underutilized Nodes found \\
        D3 & HighNodeUtilization & 407 & 23.2s & Aggressive eviction from overutilized Nodes \\
        D4 & ResourceDefragmentation & 31 & 2.0s & Targeted evictions from fragmented Nodes \\
        D5 & RDG & 410 & 21.9s & Eviction budget exhausted \\
        \hline
    \end{tabular}
    }
\end{table}

LowNodeUtilization does not evict any Pods because the cluster is already highly utilized. HighNodeUtilization and RDG both perform hundreds of evictions. In contrast, ResourceDefragmentation performs only 31 evictions, indicating that TOPSIS candidate selection is more selective and does not rely on broad churn to create placement opportunities.

\subsection{Post-Deschedule Rescheduling}
\label{sec:post_deschedule_rescheduling}

The main effectiveness metric is how many previously unscheduled Pods can be admitted after descheduling. Table~\ref{tab:post_deschedule_outcomes} summarizes this result.

\begin{table}[htbp]
    \centering
    \caption{Post-Deschedule Rescheduling Outcomes}
    \label{tab:post_deschedule_outcomes}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l l r r r r}
        \hline
        \textbf{Exp.} & \textbf{Policy} & \textbf{Evictions} & \textbf{Rescheduled} & \textbf{Final Unscheduled} & \textbf{Improvement} \\
        \hline
        D1 & None & 0 & -- & 900 & Baseline \\
        D2 & LowNodeUtilization & 0 & 69 & 827 & 8.1\% \\
        D3 & HighNodeUtilization & 407 & 113 & 803 & 10.8\% \\
        D4 & ResourceDefragmentation & 31 & 215 & \textbf{671} & \textbf{25.4\%} \\
        D5 & RDG & 410 & 176 & 707 & 21.4\% \\
        \hline
    \end{tabular}
    }
\end{table}

ResourceDefragmentation produces the lowest final unscheduled Pod count. More importantly, it reaches this result with far fewer evictions than HighNodeUtilization or RDG. This supports the claim that targeted defragmentation should be evaluated by schedulability improvement and eviction cost together, not by RII reduction alone.

\subsection{Eviction Efficiency}
\label{sec:eviction_efficiency}

Table~\ref{tab:eviction_efficiency} reports rescheduling yield, defined as the number of previously unscheduled Pods admitted per eviction.

\begin{table}[htbp]
    \centering
    \caption{Eviction Efficiency and Yield}
    \label{tab:eviction_efficiency}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Policy} & \textbf{Evictions} & \textbf{Rescheduled} & \textbf{Yield} & \textbf{Desched. Failures} \\
        \hline
        LowNodeUtilization & 0 & 69 & -- & 0 \\
        HighNodeUtilization & 407 & 113 & 0.28 & 4 \\
        ResourceDefragmentation & 31 & 215 & \textbf{6.94} & 0 \\
        RDG & 410 & 176 & 0.43 & 0 \\
        \hline
    \end{tabular}
\end{table}

HighNodeUtilization and RDG free capacity by evicting many Pods, but their rescheduling yield remains low. ResourceDefragmentation achieves the highest yield because it selects Pods whose removal is expected to correct CPU-memory imbalance and create useful placement space for previously Pending workloads.

\subsection{Final Cluster Utilization}
\label{sec:final_cluster_utilization}

Table~\ref{tab:final_cluster_utilization} compares final utilization after rescheduling.

\begin{table}[htbp]
    \centering
    \caption{Final Cluster Utilization Post-Rescheduling}
    \label{tab:final_cluster_utilization}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Experiment} & \textbf{Final CPU} & \textbf{Final Mem.} & \textbf{vs Init CPU} & \textbf{vs Init Mem.} \\
        \hline
        D1 & 96.1\% & 96.0\% & -- & -- \\
        D2 & 96.6\% & 96.6\% & +0.4\% & +0.3\% \\
        D3 & 97.3\% & 96.2\% & +1.3\% & 0.0\% \\
        D4 & \textbf{99.5\%} & \textbf{99.1\%} & \textbf{+3.4\%} & \textbf{+2.7\%} \\
        D5 & 99.1\% & 97.8\% & +2.4\% & +1.7\% \\
        \hline
    \end{tabular}
\end{table}

The TOPSIS-based ResourceDefragmentation policy reaches the highest final utilization while using the smallest number of evictions among the successful policies. This result is consistent with the intended design: the policy should minimize disruptive churn while improving the cluster's ability to admit previously unscheduled Pods.

\subsection{Execution Time}
\label{sec:execution_time}

\begin{table}[htbp]
    \centering
    \caption{Simulation Execution Time Comparison}
    \label{tab:execution_time}
    \begin{tabular}{l c c}
        \hline
        \textbf{Policy} & \textbf{Deschedule Time} & \textbf{Total Simulation Time} \\
        \hline
        None & -- & 228.0s \\
        LowNodeUtilization & 0.14s & 312.6s \\
        HighNodeUtilization & 23.2s & 273.4s \\
        ResourceDefragmentation & 2.0s & 236.3s \\
        RDG & 21.9s & 254.4s \\
        \hline
    \end{tabular}
\end{table}

ResourceDefragmentation completes the descheduling phase faster than HighNodeUtilization and RDG because it evaluates and evicts fewer Pods. The additional runtime in descheduler-enabled runs is mainly caused by the retry and rescheduling phases rather than by the TOPSIS selector itself.

\subsection{Summary of Findings}
\label{sec:summary_findings}

Table~\ref{tab:summary_descheduling_outcomes} summarizes the simulator outcomes.

\begin{table}[htbp]
    \centering
    \caption{Summary of Final Descheduling Outcomes}
    \label{tab:summary_descheduling_outcomes}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{c l r r r l}
        \hline
        \textbf{Rank} & \textbf{Policy} & \textbf{Unscheduled} & \textbf{Evictions} & \textbf{Yield} & \textbf{Verdict} \\
        \hline
        1 & ResourceDefragmentation & \textbf{671} & 31 & \textbf{6.94} & Best schedulability and efficiency \\
        2 & RDG & 707 & 410 & 0.43 & Good result with high eviction cost \\
        3 & HighNodeUtilization & 803 & 407 & 0.28 & High churn and low yield \\
        4 & LowNodeUtilization & 827 & 0 & -- & Mismatched with oversubscribed clusters \\
        5 & None & 900 & 0 & -- & Baseline control \\
        \hline
    \end{tabular}
    }
\end{table}

Overall, the simulator result indicates that the TOPSIS-based ResourceDefragmentation policy reduces unscheduled Pods more effectively than the baseline policies while requiring substantially fewer evictions. This supports the broader experiment direction: the final evaluation should emphasize observable schedulability improvement, Pending Pod reduction, and eviction efficiency, with RII used as a decision aid rather than as the only proof of fragmentation reduction.
