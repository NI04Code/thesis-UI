%-----------------------------------------------------------------------------%
\chapter{\babEmpat}
\label{bab:4}
%-----------------------------------------------------------------------------%

This chapter presents the evaluation of the Resource Defragmentation policy. The methodology in Chapter~\ref{bab:3} defines how the policy detects fragmentation, ranks candidate Pods with TOPSIS, applies a feasibility guard, and supports request-based, actual-usage, and EWMA-smoothed signals. The evaluation here proceeds in two stages: a synthetic cluster walk-through that traces the algorithm end-to-end on a small Kubernetes-shaped scenario, followed by a discrete-event simulator-based scale-out that compares the policy against established baselines on a larger heterogeneous cluster.

%-----------------------------------------------------------------------------%
\section{Resource Defragmentation Descheduler Evaluation}
\label{sec:resource_defrag_evaluation}
%-----------------------------------------------------------------------------%

This section presents the evaluation of the Resource Defragmentation policy. Following the framing in Chapter~\ref{bab:3}, the evaluation here addresses the fragmentation thread of the research questions in two related stages: a synthetic cluster walk-through that traces the algorithm end-to-end on a small Kubernetes-shaped scenario, followed by a discrete-event simulator-based scale-out that compares the policy against established baselines on a larger heterogeneous cluster.

\subsection{Evaluation Approach}
\label{sec:evaluation_rationale}

The evaluation addresses two complementary questions. Following the directed Pod eviction perspective of \citet{larsson_klein_elmroth_2023}, descheduling must improve a fragmented Kubernetes cluster after the initial kube-scheduler placement; this is request-aware because Kubernetes scheduling feasibility is based on declared resource requests. Following load-aware orchestration work by \citet{marchese2024loadaware,marchese2025enhancing}, runtime CPU and memory usage may also serve as an additional eviction-ranking signal when Pod requests do not reflect actual behavior.

Both threads share a constraint: actual usage does not replace Kubernetes scheduling feasibility. A Pod can only be admitted by the scheduler if its declared requests fit on a Node. Fragmentation outcomes are therefore validated using schedulability evidence, while runtime usage is treated as an additional ranking signal rather than as a replacement for the scheduler's request-based admission.

The synthetic walk-through (Section~\ref{sec:synthetic_walkthrough_validation}) traces the algorithm end-to-end on a small cluster with explicit numerical inputs, exposing the lifecycle from a fragmented initial state through eviction to scheduler placement of both the freed slot and the evicted Pods' replacements. The simulator-based scale-out (Section~\ref{sec:simulator_topsis_rdg_evaluation}) reproduces the Kubernetes scheduling pipeline on a larger heterogeneous cluster derived from a publicly available cluster trace, comparing the policy against established utilization-based and greedy descheduling baselines.

\subsection{RII, RRF, and Fragmentation Evidence}
\label{sec:rii_rrf_fragmentation_evidence}

The implementation uses the Resource Imbalance Index (RII) as a lightweight CPU-memory imbalance signal. RII is useful for identifying Nodes whose resource shape is skewed and for ranking candidate evictions with TOPSIS. However, RII is not treated as sufficient proof that fragmentation has been solved.

The Relative Resource Fragmentation (RRF) idea from \citet{mishra_bellur_2016} is conceptually closer to a direct fragmentation metric because it asks whether remaining capacity is usable for a target request. This research keeps RRF as evaluation context rather than replacing the implementation algorithm. The practical validation is therefore RRF-like: before descheduling, aggregate free resources must be sufficient for the target Pod while no individual Node can fit it; after descheduling, the target Pod should become schedulable or Running. The synthetic walk-through in Section~\ref{sec:synthetic_walkthrough_validation} concretizes this pattern with explicit numbers; the cluster has \(1800\,m\) of aggregate free CPU but no Node fits the \(900\,m\) probe, until two evictions free a single feasible slot.

Thus, RII and TOPSIS are used as decision signals, while the final claim is grounded in schedulability outcomes. This avoids claiming success only because an index improved.

\subsection{Synthetic Cluster Walk-Through}
\label{sec:synthetic_walkthrough_validation}

The first stage of the evaluation is a synthetic cluster walk-through whose every state can be derived analytically. The walk-through has two purposes: it traces the descheduler decisions on inputs that can be checked by hand, and it exposes the schedulability lifecycle of the probe Pod under a small range of \(MaxEvictions\) values together with the default-scheduler placement of the evicted Pods' replacements.

The scenario, RII, FSI, priority, and TOPSIS calculations are described in Section~\ref{sec:topsis_example}. The same script\footnote{Available in the custom descheduler repository under \texttt{experiments/synthetic-simulation/simulate.py}.} is reused here to extend the analysis. With the budget \(MaxEvictions = 2\), the eviction loop produces the sequence \(M\text{-}1\) from \(B\) followed by \(C\text{-}1\) from \(A\). Table~\ref{tab:topsis_final_state} reports the resulting cluster state immediately after the descheduler deletions, before the default scheduler attempts to place any replacement Pods. Node \(A\) is still above the imbalance threshold, but the eviction budget is consumed and the policy stops, which is the intended budget-respecting behavior.

\begin{table}[htbp]
    \centering
    \caption{Cluster State After ResourceDefragmentation Deletions}
    \label{tab:topsis_final_state}
    \renewcommand{\arraystretch}{1.25}
    \begin{tabular}{l l c c c c}
        \hline
        \textbf{Node} & \textbf{Pods} & \textbf{Free CPU} & \textbf{Free Mem.} & \textbf{\(RII\)} & \textbf{Notes} \\
        \hline
        \(A\) & \(3C\)    & \(800\,m\) & \(1700\,Mi\) & \(+0.45\) & still fragmented, budget exhausted \\
        \(B\) & \(2C+3M\) & \(900\,m\) & \(600\,Mi\)  & \(-0.15\) & exited fragmented list \\
        \(C\) & \(3C+2M\) & \(600\,m\) & \(900\,Mi\)  & \(+0.15\) & unchanged \\
        \hline
    \end{tabular}
\end{table}

The schedulability witness is preserved by this state. Before defragmentation no Node fits the \(900\,m / 300\,Mi\) probe, so it remains Pending despite \(1800\,m\) of aggregate free CPU. After the two evictions, Node \(B\) has exactly \(900\,m\) free CPU and \(600\,Mi\) free memory, which is sufficient for the probe under request-based admission.

To expose the schedulability lifecycle of both the probe and the evicted Pods' replacements, the script feeds the post-eviction Node states and the queue of replacement Pods plus the probe Pod to a default-kube-scheduler-style placement stage. That stage applies the \texttt{NodeResourcesFit} filter and a \texttt{LeastAllocated} score equal to the mean of the per-resource fractions \((C - (R + r))/C\), with lexicographic tiebreak between equally scored Nodes. Two scheduling orders are evaluated to expose order sensitivity: \emph{probe-first}, where the previously Pending probe is admitted before any replacement, and \emph{replacement-first}, where the ReplicaSet replacements are admitted before the probe.

Table~\ref{tab:synthetic_maxev_sweep} reports the result of sweeping \(MaxEvictions\) from \(1\) to \(5\) at the fixed threshold \(0.2\). The eviction list is the sequence emitted by the policy. The probe-first and replacement-first columns report the placement of the probe Pod under each scheduling order.

\begin{table}[htbp]
    \centering
    \caption{MaxEvictions Sweep on the Synthetic Cluster (Threshold \(=0.2\))}
    \label{tab:synthetic_maxev_sweep}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{c c l l l}
        \hline
        \textbf{\(MaxEv\)} & \textbf{Evictions} & \textbf{Eviction Sequence} & \textbf{Probe (probe-first)} & \textbf{Probe (replacement-first)} \\
        \hline
        \(1\) & \(1\) & \(M\text{-}1\) from \(B\)                             & Running on \(B\) & Running on \(B\) \\
        \(2\) & \(2\) & \(M\text{-}1/B\), \(C\text{-}1/A\)                    & Running on \(B\) & Running on \(B\) \\
        \(3\) & \(3\) & \(M\text{-}1/B\), \(C\text{-}1/A\), \(C\text{-}2/A\)  & Running on \(A\) & Running on \(B\) \\
        \(4\) & \(4\) & \(M\text{-}1/B\), \(C\text{-}1/A\), \(C\text{-}2/A\), \(C\text{-}3/A\) & Running on \(A\) & Running on \(B\) \\
        \(5\) & \(4\) & same as \(MaxEv = 4\)                                & Running on \(A\) & Running on \(B\) \\
        \hline
    \end{tabular}
    }
\end{table}

Three observations follow from this sweep. First, the row \(MaxEv = 1\) shows that a single eviction is sufficient to unblock the probe in this scenario, because the chosen Pod \(M\text{-}1\) frees exactly the \(100\,m\) of CPU that Node \(B\) needs to satisfy the \(900\,m\) probe. The descheduler does not have direct knowledge of the probe; the alignment between the TOPSIS choice and the probe's resource shape is a consequence of the imbalance-aware scoring rather than an explicit hint. Second, the row \(MaxEv = 5\) only produces four evictions, because Node \(A\) drops out of the fragmented list once the loop reaches \(RII = +0.15 \le \) threshold. \(MaxEvictions\) is therefore an upper bound, not a target. Third, the probe-first and replacement-first columns disagree from \(MaxEv = 3\) onward: when the budget overshoots, the default scheduler can place the probe on Node \(A\) if it is admitted before the replacements take the larger free-CPU slot, and on Node \(B\) otherwise. The probe still becomes Running in both orders, but the placement is not unique once the cluster has multiple feasible Nodes.

A practical consequence is that the descheduler outcome cannot be evaluated only by counting evictions. Even a budget that overshoots can be effective, but it also commits the cluster to broader churn whose final placement depends on the scheduler ordering. In this scenario the lowest-cost successful configuration is \(MaxEv = 1\); \(MaxEv = 2\) reaches the same schedulability outcome with one extra eviction; \(MaxEv \ge 3\) does not improve the binary success measure of the probe.

The scheduler stage also provides a sanity check on the failure mode that motivates the feasibility guard. In each successful row of Table~\ref{tab:synthetic_maxev_sweep}, the replacement Pods produced by the ReplicaSet (with the same request shape as the evicted Pods) are placed on Nodes other than the original origin under the \texttt{LeastAllocated} score. That outcome is favorable but not guaranteed in general; under a bin-packing score such as \texttt{MostAllocated}, replacements can return to the origin and erase the defragmentation effect. The walk-through therefore validates the policy under a default-style scheduler while documenting the dependency on the scheduler's scoring strategy as a known limitation.

\subsubsection{Actual-Usage Walk-Through}
\label{sec:actual_usage_walkthrough}

The request-based walk-through above relies on Pod requests as the imbalance signal. The actual-usage variant of the policy reads runtime CPU and memory ratios instead, which exposes a different class of fragmentation: a cluster whose declared requests look perfectly balanced but whose actual usage is concentrated on a single resource dimension. This subsection extends the walk-through to cover that case. The scenario, the per-Node initial state, and the TOPSIS step at Node \(A\) are described in Section~\ref{sec:actual_usage_walkthrough_example}; this section reports the eviction sweep and contrasts the two usage modes side by side.

Table~\ref{tab:actual_walkthrough_sweep} reports the descheduler outcome under both signals as the eviction budget grows. The request-based mode uses the same Resource Defragmentation policy with \textit{usageMode = requests}; the actual-usage mode uses \textit{usageMode = actual-raw}. \(RII_A^\star\) and \(RII_B^\star\) denote the actual-usage \(RII\) on Nodes \(A\) and \(B\) after the descheduler pass.

\begin{table}[htbp]
    \centering
    \caption{Eviction Sweep on the Hidden-Imbalance Scenario (Threshold \(=0.2\))}
    \label{tab:actual_walkthrough_sweep}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l c c l c c}
        \hline
        \textbf{Mode} & \textbf{\(MaxEv\)} & \textbf{Evictions} & \textbf{Eviction Sequence} & \textbf{\(RII_A^{\star}\)} & \textbf{\(RII_B^{\star}\)} \\
        \hline
        requests   & \(5\) & \(0\) & (none, no Node fragmented)                                              & \(+0.450\) & \(-0.450\) \\
        actual-raw & \(2\) & \(2\) & \(C^{\star}\!-\!1/A\), \(M^{\star}\!-\!1/B\)                                       & \(+0.300\) & \(-0.300\) \\
        actual-raw & \(4\) & \(4\) & \(C^{\star}\!-\!1/A\), \(M^{\star}\!-\!1/B\), \(C^{\star}\!-\!2/A\), \(M^{\star}\!-\!2/B\) & \(+0.150\) & \(-0.150\) \\
        actual-raw & \(5\) & \(4\) & same as \(MaxEv = 4\)                                                   & \(+0.150\) & \(-0.150\) \\
        \hline
    \end{tabular}
    }
\end{table}

Three observations follow from this sweep. First, the request-based mode emits no evictions even with a generous budget because the per-Node sums of declared requests are identical and therefore \(RII_n = 0\) on every Node. The fragmented set is empty before the eviction loop runs, which means a request-only signal cannot detect the runtime contention present in this cluster. Second, the actual-raw mode targets the imbalance directly. The eviction sequence alternates between the CPU-skewed and memory-skewed Nodes, removing one bursting Pod from each before revisiting the other. After two evictions the absolute \(RII\) on \(A\) and \(B\) drops from \(0.45\) to \(0.30\); after four evictions it drops to \(0.15\), at which point both Nodes leave the fragmented list. Third, the row \(MaxEv = 5\) only produces four evictions, because once both fragmented Nodes fall below the threshold the loop exits regardless of any remaining budget; \(MaxEvictions\) remains an upper bound rather than a target.

The descheduler does not change Pod requests, so the request view of the cluster is identical before and after each pass: every Node retains \(750\,m / 750\,Mi\) of free CPU and memory by request after two evictions, and \(1000\,m / 1000\,Mi\) after four evictions. The evictions therefore do not reduce the cluster's request-based capacity for incoming workloads; they shift runtime load away from the skewed dimension while leaving scheduling feasibility intact. This is the regime where actual-usage scoring contributes information that request-based scoring cannot derive on its own.

\subsubsection{EWMA Walk-Through}
\label{sec:ewma_walkthrough}

The actual-raw walk-through above demonstrates that runtime usage exposes fragmentation that request-based scoring cannot detect. However, the same actual-raw signal is sensitive to short-term spikes: a single elevated reading promotes a Node into the fragmented set even when its longer-term resource shape is balanced. The EWMA variant of the policy uses the smoothed estimate from Equation~\ref{eq:ewma_usage} as the imbalance signal, and is therefore expected to ignore transient spikes while still detecting sustained imbalance. The scenario, the per-signal initial \(RII\), and the resulting fragmented set are described in Section~\ref{sec:ewma_walkthrough_example}; this section reports the eviction sweep under both the actual-raw and EWMA signals on the same cluster.

Table~\ref{tab:ewma_walkthrough_sweep} reports the descheduler outcome as the eviction budget grows. The cluster has a sustained CPU-heavy Node \(A\) and a sustained memory-heavy Node \(C\) that are visible to both signals, and a transient CPU spike on Node \(B\) that is visible only to the actual-raw signal.

\begin{table}[htbp]
    \centering
    \caption{Eviction Sweep on the Transient-Spike Scenario (Threshold \(=0.2\))}
    \label{tab:ewma_walkthrough_sweep}
    \renewcommand{\arraystretch}{1.25}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l c c l}
        \hline
        \textbf{Mode} & \textbf{\(MaxEv\)} & \textbf{Evictions} & \textbf{Eviction Sequence} \\
        \hline
        actual-raw  & \(2\) & \(2\) & \(B\text{-}6/B\), \(A\text{-}1/A\) \\
        actual-raw  & \(3\) & \(3\) & \(B\text{-}6/B\), \(A\text{-}1/A\), \(C\text{-}1/C\) \\
        actual-raw  & \(5\) & \(5\) & \(B\text{-}6/B\), \(A\text{-}1/A\), \(C\text{-}1/C\), \(A\text{-}2/A\), \(C\text{-}2/C\) \\
        actual-ewma & \(2\) & \(2\) & \(A\text{-}1/A\), \(C\text{-}1/C\) \\
        actual-ewma & \(3\) & \(3\) & \(A\text{-}1/A\), \(C\text{-}1/C\), \(A\text{-}2/A\) \\
        actual-ewma & \(5\) & \(4\) & \(A\text{-}1/A\), \(C\text{-}1/C\), \(A\text{-}2/A\), \(C\text{-}2/C\) \\
        \hline
    \end{tabular}
    }
\end{table}

Two points stand out. First, the actual-raw mode begins by evicting \(B\text{-}6\) before reaching the genuinely fragmented Nodes \(A\) and \(C\). Node \(B\) has the highest priority in the request view because the spike concentrates almost all of its free space at zero on one resource dimension, which produces a very small \(FSI\) and a large priority value. The descheduler therefore spends the first eviction on a Pod whose imbalance is transient, and only the remaining budget is available for the sustained skews on \(A\) and \(C\). With a budget of two, both Nodes \(A\) and \(C\) still appear in the final fragmented set. Second, the actual-ewma mode skips Node \(B\) entirely because the smoothed estimate is dominated by past samples, with \(\beta = 0.9\) in the production agent which corresponds to \(\alpha = 1 - \beta = 0.1\) in the form of Equation~\ref{eq:ewma_usage}. Each eviction in the actual-ewma row contributes directly to reducing \(|RII|\) on a sustainably fragmented Node, and a budget of two already removes the leading Pod from each of \(A\) and \(C\). With a budget of five, the loop terminates after four evictions because both \(A\) and \(C\) have dropped below the threshold; \(MaxEvictions\) remains an upper bound rather than a target.

This behavior is the intended trade-off of the smoothed signal. The EWMA mode requires runtime imbalance to persist across several samples before it shifts the eviction decision, so it ignores spikes whose duration is short relative to the smoothing horizon. The same property also slows down the response to genuine workload shifts; that limitation is acknowledged here and revisited as a tuning consideration in the simulator-based evaluation.

\subsection{Simulator-Based Scale-Out}
\label{sec:simulator_topsis_rdg_evaluation}

The second stage of the evaluation scales the analysis to a larger cluster using a discrete-event Kubernetes simulator. This stage focuses on the request-based TOPSIS-RDG behavior under a real-world workload trace and compares it with common utilization-based descheduling strategies and the RDG baseline.

\subsubsection{Experimental Setup}
\label{sec:experimental_setup}

The simulator reproduces the Kubernetes scheduling pipeline, including filtering, scoring, reservation, and binding phases. Using a simulator allows the same workload trace and cluster topology to be repeated across policies without the noise introduced by live-cluster timing, autoscaling, or external workload variation.

\noindent\textbf{Workload: Google Borg Cluster Trace.}\quad \label{sec:borg_workload}

The experimental workload is derived from the publicly available Google Borg cluster trace. Task submission events are converted into Pod-like workload objects and scaled against a reference machine of 96 vCPUs and 384 GiB memory.

To create multi-dimensional contention, Pods are classified into three strata through stratified sampling: \texttt{HIGH\_CPU}, \texttt{HIGH\_MEM}, and \texttt{BALANCED}. Resource requests are then scaled so that the total demand exceeds the simulated cluster capacity on both CPU and memory dimensions. This creates an initial set of unscheduled Pods that can be used to evaluate whether descheduling improves later schedulability.

\noindent\textbf{Cluster Topology.}\quad \label{sec:cluster_topology}

The simulated cluster consists of 20 heterogeneous Nodes divided into three capacity tiers, as shown in Table~\ref{tab:cluster_topology}. The tiered topology is used to avoid an overly uniform bin-packing problem and to create realistic differences in Pod placement options.

\begin{table}[htbp]
    \centering
    \caption{Simulated Cluster Node Topology}
    \label{tab:cluster_topology}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{l c c c c}
        \hline
        \textbf{Tier} & \textbf{vCPUs (milliCPU)} & \textbf{Memory (MiB)} & \textbf{Weight} & \textbf{Expected Count} \\
        \hline
        Small & 16000 & 65536 & 30\% & 6 Nodes \\
        Medium & 32000 & 131072 & 40\% & 8 Nodes \\
        Large & 64000 & 262144 & 30\% & 6 Nodes \\
        \hline
    \end{tabular}
\end{table}

\noindent\textbf{Policies Under Evaluation.}\quad \label{sec:policies_under_evaluation}

Five simulator configurations are evaluated:

\begin{itemize}
    \item \textbf{D1 -- No Descheduler.} Baseline control with no descheduling.
    \item \textbf{D2 -- LowNodeUtilization.} Official utilization policy that evicts Pods from underutilized Nodes to consolidate load.
    \item \textbf{D3 -- HighNodeUtilization.} Official utilization policy that evicts Pods from overutilized Nodes to spread load.
    \item \textbf{D4 -- ResourceDefragmentation.} The proposed TOPSIS-based multi-criteria eviction strategy.
    \item \textbf{D5 -- RDG.} The Resource Defragmentation Greedy strategy proposed by Guo et al.~\citep{guo2025joint}.
\end{itemize}

All descheduler-enabled runs use the same eviction budget so that policy effectiveness is not explained by allowing one policy to evict substantially more Pods than another.

\subsection{Results and Analysis}
\label{sec:results_analysis}

\subsubsection{Initial Scheduling}
\label{sec:initial_scheduling_results}

Table~\ref{tab:init_scheduling} shows the initial scheduling outcome before descheduling. Because the workload is intentionally oversubscribed, every policy starts with a comparable number of unscheduled Pods.

\begin{table}[htbp]
    \centering
    \caption{Initial Scheduling Phase Results}
    \label{tab:init_scheduling}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Experiment} & \textbf{Pods Scheduled} & \textbf{Pods Failed} & \textbf{CPU Alloc.} & \textbf{Memory Alloc.} \\
        \hline
        D1 & 4100 & 900 & 96.1\% & 96.0\% \\
        D2 & 4104 & 896 & 96.2\% & 96.3\% \\
        D3 & 4088 & 912 & 96.0\% & 96.2\% \\
        D4 & 4114 & 886 & 96.1\% & 96.4\% \\
        D5 & 4117 & 883 & 96.7\% & 96.1\% \\
        \hline
    \end{tabular}
\end{table}

The initial phase confirms that the workload creates similar pressure across all runs. Therefore, later differences in unscheduled Pod count, eviction count, and rescheduling yield can be attributed to descheduler behavior rather than to a substantially different initial workload.

\subsubsection{Descheduling Behavior}
\label{sec:descheduling_behavior}

The descheduling phase reveals the cost of each policy in terms of evictions and runtime, as shown in Table~\ref{tab:descheduling_behavior}.

\begin{table}[htbp]
    \centering
    \caption{Descheduling Phase Behavior}
    \label{tab:descheduling_behavior}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l l r r l}
        \hline
        \textbf{Exp.} & \textbf{Policy} & \textbf{Evictions} & \textbf{Time} & \textbf{Observation} \\
        \hline
        D1 & None & 0 & -- & No descheduling performed \\
        D2 & LowNodeUtilization & 0 & 0.14s & No underutilized Nodes found \\
        D3 & HighNodeUtilization & 407 & 23.2s & Aggressive eviction from overutilized Nodes \\
        D4 & ResourceDefragmentation & 31 & 2.0s & Targeted evictions from fragmented Nodes \\
        D5 & RDG & 410 & 21.9s & Eviction budget exhausted \\
        \hline
    \end{tabular}
    }
\end{table}

LowNodeUtilization does not evict any Pods because the cluster is already highly utilized. HighNodeUtilization and RDG both perform hundreds of evictions. In contrast, ResourceDefragmentation performs only 31 evictions, indicating that TOPSIS candidate selection is more selective and does not rely on broad churn to create placement opportunities.

\subsubsection{Post-Deschedule Rescheduling}
\label{sec:post_deschedule_rescheduling}

The main effectiveness metric is how many previously unscheduled Pods can be admitted after descheduling. Table~\ref{tab:post_deschedule_outcomes} summarizes this result.

\begin{table}[htbp]
    \centering
    \caption{Post-Deschedule Rescheduling Outcomes}
    \label{tab:post_deschedule_outcomes}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l l r r r r}
        \hline
        \textbf{Exp.} & \textbf{Policy} & \textbf{Evictions} & \textbf{Rescheduled} & \textbf{Final Unscheduled} & \textbf{Improvement} \\
        \hline
        D1 & None & 0 & -- & 900 & Baseline \\
        D2 & LowNodeUtilization & 0 & 69 & 827 & 8.1\% \\
        D3 & HighNodeUtilization & 407 & 113 & 803 & 10.8\% \\
        D4 & ResourceDefragmentation & 31 & 215 & \textbf{671} & \textbf{25.4\%} \\
        D5 & RDG & 410 & 176 & 707 & 21.4\% \\
        \hline
    \end{tabular}
    }
\end{table}

ResourceDefragmentation produces the lowest final unscheduled Pod count. More importantly, it reaches this result with far fewer evictions than HighNodeUtilization or RDG. This supports the claim that targeted defragmentation should be evaluated by schedulability improvement and eviction cost together, not by RII reduction alone.

\subsubsection{Eviction Efficiency}
\label{sec:eviction_efficiency}

Table~\ref{tab:eviction_efficiency} reports rescheduling yield, defined as the number of previously unscheduled Pods admitted per eviction.

\begin{table}[htbp]
    \centering
    \caption{Eviction Efficiency and Yield}
    \label{tab:eviction_efficiency}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Policy} & \textbf{Evictions} & \textbf{Rescheduled} & \textbf{Yield} & \textbf{Desched. Failures} \\
        \hline
        LowNodeUtilization & 0 & 69 & -- & 0 \\
        HighNodeUtilization & 407 & 113 & 0.28 & 4 \\
        ResourceDefragmentation & 31 & 215 & \textbf{6.94} & 0 \\
        RDG & 410 & 176 & 0.43 & 0 \\
        \hline
    \end{tabular}
\end{table}

HighNodeUtilization and RDG free capacity by evicting many Pods, but their rescheduling yield remains low. ResourceDefragmentation achieves the highest yield because it selects Pods whose removal is expected to correct CPU-memory imbalance and create useful placement space for previously Pending workloads.

\subsubsection{Final Cluster Utilization}
\label{sec:final_cluster_utilization}

Table~\ref{tab:final_cluster_utilization} compares final utilization after rescheduling.

\begin{table}[htbp]
    \centering
    \caption{Final Cluster Utilization Post-Rescheduling}
    \label{tab:final_cluster_utilization}
    \begin{tabular}{l r r r r}
        \hline
        \textbf{Experiment} & \textbf{Final CPU} & \textbf{Final Mem.} & \textbf{vs Init CPU} & \textbf{vs Init Mem.} \\
        \hline
        D1 & 96.1\% & 96.0\% & -- & -- \\
        D2 & 96.6\% & 96.6\% & +0.4\% & +0.3\% \\
        D3 & 97.3\% & 96.2\% & +1.3\% & 0.0\% \\
        D4 & \textbf{99.5\%} & \textbf{99.1\%} & \textbf{+3.4\%} & \textbf{+2.7\%} \\
        D5 & 99.1\% & 97.8\% & +2.4\% & +1.7\% \\
        \hline
    \end{tabular}
\end{table}

The TOPSIS-based ResourceDefragmentation policy reaches the highest final utilization while using the smallest number of evictions among the successful policies. This result is consistent with the intended design: the policy should minimize disruptive churn while improving the cluster's ability to admit previously unscheduled Pods.

\subsubsection{Execution Time}
\label{sec:execution_time}

\begin{table}[htbp]
    \centering
    \caption{Simulation Execution Time Comparison}
    \label{tab:execution_time}
    \begin{tabular}{l c c}
        \hline
        \textbf{Policy} & \textbf{Deschedule Time} & \textbf{Total Simulation Time} \\
        \hline
        None & -- & 228.0s \\
        LowNodeUtilization & 0.14s & 312.6s \\
        HighNodeUtilization & 23.2s & 273.4s \\
        ResourceDefragmentation & 2.0s & 236.3s \\
        RDG & 21.9s & 254.4s \\
        \hline
    \end{tabular}
\end{table}

ResourceDefragmentation completes the descheduling phase faster than HighNodeUtilization and RDG because it evaluates and evicts fewer Pods. The additional runtime in descheduler-enabled runs is mainly caused by the retry and rescheduling phases rather than by the TOPSIS selector itself.

\subsubsection{Summary of Findings}
\label{sec:summary_findings}

Table~\ref{tab:summary_descheduling_outcomes} summarizes the simulator outcomes.

\begin{table}[htbp]
    \centering
    \caption{Summary of Final Descheduling Outcomes}
    \label{tab:summary_descheduling_outcomes}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{c l r r r l}
        \hline
        \textbf{Rank} & \textbf{Policy} & \textbf{Unscheduled} & \textbf{Evictions} & \textbf{Yield} & \textbf{Verdict} \\
        \hline
        1 & ResourceDefragmentation & \textbf{671} & 31 & \textbf{6.94} & Best schedulability and efficiency \\
        2 & RDG & 707 & 410 & 0.43 & Good result with high eviction cost \\
        3 & HighNodeUtilization & 803 & 407 & 0.28 & High churn and low yield \\
        4 & LowNodeUtilization & 827 & 0 & -- & Mismatched with oversubscribed clusters \\
        5 & None & 900 & 0 & -- & Baseline control \\
        \hline
    \end{tabular}
    }
\end{table}

Overall, the simulator result indicates that the TOPSIS-based ResourceDefragmentation policy reduces unscheduled Pods more effectively than the baseline policies while requiring substantially fewer evictions. This supports the broader experiment direction: the final evaluation should emphasize observable schedulability improvement, Pending Pod reduction, and eviction efficiency, with RII used as a decision aid rather than as the only proof of fragmentation reduction.
%-----------------------------------------------------------------------------%
\section{Latency and Defragmentation Trade-off Test}
\label{sec:netaware4_test}
%-----------------------------------------------------------------------------%
In this section we evaluates the trade-off between network latency preservation and resource defragmentation within a Kubernetes cluster using the proposed Network-Aware plugin. The evaluation focuses on how the plugin maintains communication efficiency by preventing pod evictions that could potentially increase network latency. The experiments are conducted using three balancing strategies, namely LowNodeUtilization, HighNodeUtilization, and custom ImbalanceIndexUtilization, to analyze the impact of network-aware eviction filtering under different workload redistribution approaches.

%-----------------------------------------------------------------------------%
\subsection{Experimental Setup and Environment Configuration}
\label{subsec:netaware4_experimentsetup}
%-----------------------------------------------------------------------------%
To evaluate the effectiveness of the proposed Network-Aware Descheduler against resource fragmentation and degraded latency anomalies, we built a physical-to-logical hybrid testing sandbox. The design validates descheduling actions against varying node performance tiers under tight resource ceilings, providing deterministic telemetry data for thesis synthesis.

%-----------------------------------------------------------------------------%
\subsubsection{Infrastructure and Cluster Topology}
\label{subsubsec:netaware4_infrasetup}
%-----------------------------------------------------------------------------%
The experimental testing cluster is hosted inside an isolated Virtual Private Cloud (VPC) on Amazon Web Services (AWS). Due to the structural restrictions of the cloud sandbox environment, all instances are physically provisioned within the \textit{us-east-1} geographic region. The infrastructure pool comprises five distinct \textit{t3.micro} virtual machines acting as worker nodes, alongside a dedicated administrative control plane and one worker specific for Prometheus.  

A critical engineering constraint of the \textit{t3.micro} hardware profile is its highly restricted footprint: each instance features 2 vCPUs and a bare 1 GiB (1024 MiB) of physical RAM. After accounting for vital system daemon overheads (including the Linux kernel, Kubelet, container runtime, and Flannel network plugins), the net allocatable memory per node drops to a constrained window of approximately 800 MiB.  

To model real-world high-availability environments, the underlying physical cluster nodes are distributed across distinct Availability Zones (AZs) as follows:  

\begin{itemize}
    \item \textit{us-east-1d}: worker-1, worker-2
    \item \textit{us-east-1b}: worker-3
    \item \textit{us-east-1a}: worker-4, worker-5
\end{itemize}

%-----------------------------------------------------------------------------%
\subsubsection{Network Profile and Topology Modeling}
\label{subsubsec:netaware4_networksetup}
%-----------------------------------------------------------------------------%
To evaluate the impact of network locality on scheduling and descheduling decisions, the cluster topology was configured to include nodes with heterogeneous network characteristics distributed across multiple logical availability zones:

\begin{itemize}
    \item \textbf{Normal Network Conditions:} Consists of worker-1, worker-2, and worker-3. These nodes retain their native cloud-provider network performance. Communication within the same availability zone experiences approximately 0.3 ms latency, while communication across availability zones incurs a natural latency of approximately 1-2 ms.

    \item \textbf{Degraded Network Conditions:} Consists of worker-4 and worker-5, which are assigned to a separate logical availability zone and configured to emulate constrained network conditions. These nodes experience significantly higher communication latency than the rest of the cluster, representing a lower-quality network domain within the topology.
\end{itemize}

To achieve the network degradation, the primary operating system network interface (\emph{ens5}) on both worker-4 and worker-5 was targeted. A deterministic outbound network emulation queue discipline (\emph{qdisc}) rule was applied directly to the root interface using the Linux Traffic Control (\emph{tc}) framework:

\begin{lstlisting}[language=bash, numbers=none, xleftmargin=0.5em, framexleftmargin=0em]
sudo tc qdisc add dev ens5 root netem delay 80ms 5ms distribution normal
\end{lstlisting}

Crucially, because this traffic shaping rule acts as a uniform egress filter, it creates a multi-hop latency matrix depending on the communication path. When a normal node (worker-1) initiates a round-trip connection with a degraded node (worker-4), the packet triggers an 80 ms delay leaving worker-4's interface during the response phase. However, when worker-4 and worker-5 communicate with each other, both machines enforce their outbound egress rules. This creates an additive round-trip time (RTT) accumulation:

\[
RTT_{\text{Tier2}\leftrightarrow\text{Tier2}} = Delay_{\text{Node4 (Egress)}} + Delay_{\text{Node5 (Egress)}} \approx 160\,\text{ms}
\]


%-----------------------------------------------------------------------------%
\subsubsection{Telemetry and Monitoring Architecture}
\label{subsubsec:netaware4_monitoringsetup}
%-----------------------------------------------------------------------------%
To provide the network-aware descheduler with continuous data streams, we deployed a tight telemetry loop combining a full-mesh prober with a time-series database.

Bloomberg's Goldpinger (version 3.10.1) was deployed as a cluster-wide DaemonSet, pinning a network auditing pod to each of the five worker nodes. Every 30 seconds, each Goldpinger instance executes a cross-node internal web check to every discovered peer over port 8080. This creates an active, real-time matrix of the data paths between all five nodes.

To capture and aggregate this data, a minimal installation of Prometheus was deployed using kube-prometheus-stack Helm chart. Because of the strict memory limits of the \textit{t3.micro} nodes, we limit the monitoring stack to prevent system crashes. High-overhead default dependencies including Grafana, Alertmanager, kube-state-metrics, and node-exporter were entirely disabled.

The Prometheus server instance was pinned to worker-6 (dedicated Prometheus node) using a targeted \emph{nodeSelector}. Its operational resource limits were tightly bounded to prevent memory leakage from exhausting the node's approximately 800 MiB allocatable ceiling:

\lstinputlisting[
    caption={Prometheus resources config for Latency and Defragmentation Trade-off Test}, 
    label={lst:prometheus_config},
]{assets/codes/pseudos/netaware/prometheusconfig.psu}

By expanding the global scrapeInterval to 45s and restricting data retention to a lean 6h window, the internal time-series indexing map remains small enough to sit safely in 800 MiB usable RAM. Telemetry target discovery is governed by a native PodMonitor custom resource deployed to the \emph{prom} namespace. This monitor hooks directly into the named container exposed in the Goldpinger daemon manifest, automatically mapping the scraping loop to target port 8080.

The telemetry data is collected by evaluating two core histogram vectors: \textit{goldpinger\_peers\_response\_time\_s\_sum} and \textit{goldpinger\_peers\_response\_time\_s\_count}. The network-aware descheduler will queries these metrics using an integrated PromQL engine over a rolling 5-minute mathematical average latency via the following unified query string:

\[
\text{Network Latency (Seconds)} = \frac{\text{rate}(\text{goldpinger\_peers\_response\_time\_s\_sum}[5\text{m}])}{\text{rate}(\text{goldpinger\_peers\_response\_time\_s\_count}[5\text{m}])} 
\]

The query utilizes host name as its source indicator and peer name as its target label mapping. This allows the descheduler to programmatically construct a real-time topology map of the cluster's network characteristics.

%-----------------------------------------------------------------------------%
\subsection{Test Scenarios and Methodology}
\label{subsec:netaware4_testscenarios}
%-----------------------------------------------------------------------------%
Continuing Section \ref{subsec:netaware4_experimentsetup}, we created an automated end-to-end testing framework. The methodology relies on generating reproducible resource utilization imbalances across the cluster's network boundaries and observing whether the descheduler can optimize cluster fragmentation without routing latency-sensitive pods across simulated wide-area network links.


%-----------------------------------------------------------------------------%
\subsubsection{Workload Profiling}
\label{subsubsec:netaware4_workloadprofiling}
%-----------------------------------------------------------------------------%
The test workloads are isolated within a dedicated Kubernetes namespace (\emph{test-app}). To ensure that scheduling changes register clearly on the resource-constrained t3.micro worker nodes (approximately 1800m allocatable CPU and 700 MiB usable memory per node), the container resource requests were strategically scaled up to form a volatile footprint.

The application layer simulates a multi-tier e-commerce microservice architecture broken down into two distinct categories:

\begin{enumerate}
    \item \textbf{Network Dependent Pods:} A tightly coupled microservice chain consisting of 5 replicas across three application architectures:
    
    \begin{itemize}
        \item \textbf{Payment Deployment:} Each requesting 250m vCPU and 64 MiB of memory.
        
        \item \textbf{Transaction Deployment:} Each requesting 200m vCPU and 48 MiB of memory.
        
        \item \textbf{Database Deployment:} Each requesting 250m vCPU and 96 MiB of memory.
    \end{itemize}

    Every pod within these deployments is stamped with an explicit metadata label/network-group: \textit{checkout-flow} to inform our descheduler that these components are latency-sensitive and share cross-service dependencies.

    \item \textbf{Filler Pods:} A decoupled background workload, each requesting 150m vCPU and 48 MiB of memory. These pods do not possess a \textit{network-group} label. They act as network-agnostic, generic balancing targets designed to absorb raw node capacity.
\end{enumerate}

The combined workload table for each scenario is defined below:

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{%
        \begin{tabular}{lcccc}
            \toprule
            \textbf{Deployment} & \textbf{Replicas} & \textbf{CPU/Mem per Pod} & \textbf{Total CPU/Mem Req.} & \textbf{network-group Label} \\ 
            \midrule
            Payment             & 2                 & 250m/64 MiB              & 500m/128 MiB                & checkout-flow \\
            Transaction         & 2                 & 200m/48 MiB              & 400m/96 MiB                 & checkout-flow \\
            Database (DB)       & 1                 & 250m/96 MiB              & 250m/96 MiB                 & checkout-flow \\
            Filler Pods         & 4                 & 150m/48 MiB              & 600m/192 MiB                & None          \\ 
            \midrule
            \textbf{Total Workload} & \textbf{9}     & ---                  & \textbf{1750m/512 MiB}         & ---           \\
            \bottomrule
        \end{tabular}%
    }
    \caption{LowNodeUtilization/HighNodeUtilization Resource allocation for Latency and Defragmentation Trade-off Test}
    \label{tab:netaware4_workloadprofiling1}
\end{table}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Deployment} & \textbf{Replicas} & \textbf{CPU/Mem per Pod} & \textbf{Total CPU/Mem Req.} & \textbf{network-group Label} \\ 
        \midrule
        Payment             & 2                 & 100m / 100 MiB           & 200m / 200 MiB              & checkout-flow \\
        Transaction         & 1                 & 50m / 500 MiB            & 50m / 500 MiB               & checkout-flow \\
        Database (DB)       & 1                 & 1000m / 50 MiB           & 1000m / 50 MiB              & checkout-flow \\
        Filler Pods         & 0                 & ---                      & ---                         & None          \\ 
        \midrule
        \textbf{Total Workload} & \textbf{4}    & ---                      & \textbf{1250m / 750 MiB}    & ---           \\
        \bottomrule
    \end{tabular}
    \caption{ImbalanceIndexUtilization Resource Allocation for Latency and Defragmentation Trade-off Test}
    \label{tab:netaware4_workloadprofiling2}
\end{table}

%-----------------------------------------------------------------------------%
\subsubsection{Workload Initial State Distribution}
\label{subsubsec:netaware4_workloaddistribution}
%-----------------------------------------------------------------------------%
After doing workload profiling, we initialize two automated initial distribution designed to create controlled load imbalance scenarios:
\begin{itemize}
    \item \textbf{LowNodeUtilization Initial State:} The nodes worker-3, worker-4, and worker-5 are temporarily cordoned before workload deployment. As a result, the default Kubernetes scheduler is forced to pack all 9 pods onto the remaining high-performance nodes, worker-1 and worker-2. After the workloads stabilize, the previously cordoned nodes are uncordoned. This creates a strongly imbalanced cluster state where the active nodes reach approximately 44\%--53\% CPU utilization, while the remaining nodes stay at 0\%, exceeding the configured 30\% target and 10\% low-utilization thresholds.

    \item \textbf{HighNodeUtilization Initial State:} Additional filler pods will be scheduled to worker-1 and worker-2 and the other pods are uniformly distributed over all 5 nodes resulting in fragmented cluster state where node utilization for worker-1 and worker-2 are above defined threshold of 20\% and the other workers are below approximately 14\%.

     \item \textbf{ImbalanceIndexUtilization Initial State:} Workloads are intentionally pinned to create asymmetric resource utilization across the cluster nodes. The CPU-intensive Database pod together with the localized Payment pods are concentrated on worker-2, producing a CPU-dominant utilization profile with approximately 60\% CPU allocation and 30\% memory allocation. At the same time, the memory-intensive Transaction pod is placed on the network-degraded worker-5, creating a memory-dominant profile with approximately 2.5\% CPU allocation and 61\% memory allocation. This configuration establishes a scenario where exchanging pods between nodes would mathematically improve the cluster’s multi-dimensional resource balance, but at the cost of degrading application network locality.
\end{itemize}

%-----------------------------------------------------------------------------%
\subsubsection{Experimental Matrix and Group Testing}
\label{subsubsec:netaware4_experimentalmatrix}
%-----------------------------------------------------------------------------%
The evaluation is split into three experimental testing scenario for LowNodeUtilization, HighNodeUtilization, and ImbalanceIndexUtilization. To ensure statistical validity and eliminate variations from container runtime cold starts or scheduling race conditions, we also run each scenario 3 times. For each scenario we test combination of strategies as shown in the experimental matrix defined below:

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{%
       \begin{tabular}{lccc}
            \toprule
            \textbf{Strategy / Scenario} & \textbf{Low Node Util.} & \textbf{High Node Util.} & \textbf{Imbalance Index Util.} \\ 
            \midrule
            Baseline                     & \checkmark              & \checkmark               & \checkmark                     \\
            Topology L1                  & \checkmark              & \checkmark               & \checkmark                     \\
            Topology L2                  & \checkmark              & \checkmark               & ---                            \\
            Topology L1 \& L2            & \checkmark              & \checkmark               & ---                            \\
            Latency L1                   & \checkmark              & \checkmark               & \checkmark                     \\
            Latency L2                   & \checkmark              & \checkmark               & ---                            \\
            Latency L1 \& L2             & \checkmark              & \checkmark               & ---                            \\
            \midrule
        \end{tabular}
    }
    \caption{Experimental Matrix Mapping for Latency and Defragmentation Trade-off Test}
    \label{tab:netaware4_experimentalmatrix}
\end{table}

We also configure specific plugin configuration for each scenario as follows:

\begin{itemize}
    \item \textbf{Low Node Utilization:} This group utilizes a strict resource boundary constraint where any node under 10\% utilization is marked as underutilized, while any node exceeding 30\% is classified as overutilized.

    \item \textbf{High Node Utilization:} This group performs the inverse scheduling optimization by identifying nodes with less than 20\% resource utilization as consolidation candidates to support physical host downscaling.

    \item \textbf{Imbalance Index Utilization:} This group utilizes 0.15\% Imbalance Index Threshold and limit eviction to 50 pods as consolidation candidates to support physical host downscaling.
\end{itemize}

%-----------------------------------------------------------------------------%
\subsection{Evaluation Metrics}
\label{subsec:netaware4_evaluationmetrics}
%-----------------------------------------------------------------------------%
To quantitatively measure and compare performance trade-offs, and systemic impact of the developed Network-Aware Descheduler against the standard upstream baseline, we establish a multi-dimensional evaluation framework. The success of the various descheduling configurations is assessed using four core metric categories that systematically capture algorithmic behavior, cluster resource redistribution efficiency, network infrastructure locality, and structural topology alignment.

%-----------------------------------------------------------------------------%
\subsubsection{M1: Eviction Count}
\label{subsubsec:netaware4_evaluationmetrics1}
%-----------------------------------------------------------------------------%
The Eviction Count serves as a direct indicator of algorithmic activity and work consolidation aggressiveness within a single execution sweep. It counts evicted pods from their original host nodes. Information for this metric is extracted straight from the single-shot job log by quantifying the total occurrence of specific target eviction notification strings:

\[
E_{count} = \sum_{j \in J} e_j
\]

Where:

\begin{itemize}
    \item \(E_{count}\) is the total number of pods evicted during a specific descheduling sweep.
    
    \item \(J\) represents the set of log entries recorded during the execution job lifecycle.
    
    \item \(e_j\) is a binary flag equal to \(1\) if log entry \(j\) matches the string \textit{"Evicted pod"}, and \(0\) otherwise.
\end{itemize}

From an architectural standpoint, network-aware configurations are structurally expected to show a decreased eviction profile compared to the unrestricted baseline. This contraction representatively isolates the "cost of network-awareness," where the integrated cost filters purposely block rebalancing movements that would otherwise degrade inter-service network latency bounds.

%-----------------------------------------------------------------------------%
\subsubsection{M2: Defragmentation Standard Deviation and Active Node Count}
\label{subsubsec:netaware4_evaluationmetrics2}
%-----------------------------------------------------------------------------%
To properly evaluate and distinguish between the opposing structural goals of load defragmentation and bin-packing, we create a standard framework to measure cluster resource standard deviation. The evaluation is performed independently across both CPU and memory resource dimensions, where the utilization coefficient \(U_{n,R}\) for each worker node \(n\) and resource dimension \(R \in \{\text{cpu}, \text{memory}\}\) is defined as follows:

\[
U_{n,R}
=
\left(
\frac{R_{\text{req}}(n)}
{R_{\text{alloc}}(n)}
\right)
\times 100\%
\]

Where:

\begin{itemize}
    \item \(U_{n,R}\) represents the request-based utilization percentage of node \(n\) for resource \(R\).

    \item \(R_{\text{req}}(n)\) denotes the total requested amount of resource \(R\) from all pods assigned to node \(n\).

    \item \(R_{\text{alloc}}(n)\) denotes the allocatable capacity of node \(n\) for resource \(R\), as reported by the Kubelet API.
\end{itemize}

Using these utilization coefficients, the workload distribution variance across the cluster is modeled using the population standard deviation \(\sigma_R\):

\[
\sigma_R
=
\sqrt{
\frac{1}{N}
\sum_{n=1}^{N}
(U_{n,R} - \mu_R)^2
}
\]

Where:

\begin{itemize}
    \item \(\sigma_R\) is the population standard deviation of resource utilization for resource \(R\).

    \item \(N\) represents the total number of worker nodes in the cluster (\(N=5\)).

    \item \(\mu_R\) is the mean utilization percentage for resource \(R\) computed across all nodes.
\end{itemize}

Simultaneously, we also track the Active Node Count (\(N_{\text{active}}\)). A node is classified as active only if it hosts non-DaemonSet application workloads, excluding immutable infrastructure services such as Goldpinger, kube-proxy, and Flannel that cannot be redistributed by the descheduler.

\[
N_{\text{active}}
=
\sum_{n=1}^{N} a_n
\]

\[
a_n =
\begin{cases}
1, & \text{if } \sum R_{\text{workload}}(n) > 0 \\
0, & \text{otherwise}
\end{cases}
\]

Where:

\begin{itemize}
    \item \(N_{\text{active}}\) is the total number of worker nodes running application workloads.

    \item \(a_n\) is the binary activity indicator for node \(n\).

    \item \(R_{\text{workload}}(n)\) represents the compute requests generated by non-DaemonSet workloads assigned to node \(n\).
\end{itemize}

By combining \(\sigma_R\) and \(N_{\text{active}}\), the framework evaluates whether the descheduling policy achieves its intended infrastructure objective. A low \(\sigma_R\) together with a high \(N_{\text{active}}\) indicates successful defragmentation, where workloads are distributed evenly across the cluster infrastructure. Contrary, a high \(\sigma_R\) combined with a low \(N_{\text{active}}\) indicates successful bin-packing, where workloads are consolidated onto a smaller subset of nodes while leaving other nodes idle and available for scale-down. An inefficient cluster state is characterized by both high \(\sigma_R\) and high \(N_{\text{active}}\), indicating that workloads remain unevenly distributed while still occupying most of the infrastructure footprint.

%-----------------------------------------------------------------------------%
\subsubsection{M3: Average Cross-Service Latency}
\label{subsubsec:netaware4_evaluationmetrics3}
%-----------------------------------------------------------------------------%
To assess the operational impact of descheduling decisions on microservice communication pathways, the Average Cross-Service Latency is measured. This metric evaluates the communication cost between interacting components that belong to the same labeled network group. To avoid bias from intra-deployment communication, the framework excludes pods managed by the same deployment owner, ensuring that only true cross-service dependencies are evaluated.

\[
P_{\text{pairs}}
=
\{
(p_1, p_2)
\mid
p_1 \in S_x,\;
p_2 \in S_y,\;
S_x \neq S_y
\}
\]

\[
C_{\text{total}}
=
\sum_{(p_1, p_2) \in P_{\text{pairs}}}
L\left(
N(p_1),
N(p_2)
\right)
\]

\[
L_{\text{avg}}
=
\frac{C_{\text{total}}}
{|P_{\text{pairs}}|}
\]

Where:

\begin{itemize}
    \item \(P_{\text{pairs}}\) represents the set of all unique cross-service pod pairs belonging to the same network group.

    \item \(p_1\) and \(p_2\) are running pods belonging to different services (\(S_x\) and \(S_y\)) within the \emph{checkout-flow} application group.

    \item \(C_{\text{total}}\) is the accumulated communication cost across all evaluated service-to-service connections.

    \item \(L(a,b)\) represents the network latency between node \(a\) and node \(b\), derived from a 5-minute rolling average collected through Goldpinger telemetry.

    \item \(N(p)\) is a mapping function that returns the worker node hosting pod \(p\).

    \item \(L_{\text{avg}}\) represents the average cross-service latency across the evaluated application mesh.
\end{itemize}

%-----------------------------------------------------------------------------%
\subsubsection{M4: Network Degraded Pod Count}
\label{subsubsec:netaware4_evaluationmetrics4}
%-----------------------------------------------------------------------------%
The final validation metric for this test is the Network Degraded Pod Count, which tracks the logical isolation boundaries of the cluster. This metric measure how many latency-sensitive pods are placed onto the network-degraded nodes.

\[
S_{\text{degraded}}
=
\sum_{p \in G_{\text{checkout}}} f(p)
\]

\[
f(p)
=
\begin{cases}
1, & \text{if } N(p) \in D_{\text{tier}} \\
0, & \text{otherwise}
\end{cases}
\]

Where:

\begin{itemize}
    \item \(S_{\text{degraded}}\) is the total number of pod placed on degraded nodes, observed during a descheduling sweep.

    \item \(G_{\text{checkout}}\) represents the set of active pods with network-group label \textit{checkout-flow} identifier.

    \item \(f(p)\) is a binary placement evaluation function for pod \(p\).

    \item \(N(p)\) returns the worker node hosting pod \(p\).

    \item \(D_{\text{tier}}\) represents the set of simulated network-degraded nodes (worker-4 and worker-5).
\end{itemize}

An un-aware baseline policy may produce an \(S_{\text{degraded}}\) value greater than zero if relocating a \emph{checkout-flow} pod to an network degraded node (worker-4 and worker-5). In contrast, an ideal successful scenario of network-aware policy should maintain \(S_{\text{degraded}} = 0\), demonstrating that latency-sensitive workloads remain confined to the high-performance node while only generic filler workloads are allowed to migrate to the degraded node tier.

%-----------------------------------------------------------------------------%
\subsection{Experimental Results: LowNodeUtilization Scenario}
\label{subsec:netaware4_resultanalysis1}
%-----------------------------------------------------------------------------%
The primary objective of the network-aware descheduler is to maintain optimal cluster resource utilization without severely degrading the communication latency of sensitive workloads. To validate this, we execute the \textit{LowNodeUtilization} scenario across three distinct strategies: a baseline (no network awareness), topology-aware filtering, and the proposed latency-aware filtering. 

Table \ref{tab:low_node_results} presents the raw results of the evaluation using the optimal configuration (\textbf{MinBetterPercentage = 75\%}). 

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{l c c c c r c}
        \toprule
        \textbf{Scenario} & \textbf{Evicted Pods} & \textbf{CPU StdDev (\%)} & \textbf{MEM StdDev (\%)} & \textbf{Active Nodes} & \textbf{Avg Latency} & \textbf{Degraded Pods} \\
        \midrule
        a1: Baseline & 10 & 23.6 $\rightarrow$ 15.7 & 23.5 $\rightarrow$ 13.9 & 5 & 61.717 ms & 1 \\
        a2: Topology L1 & 10 & 23.6 $\rightarrow$ 15.7 & 23.5 $\rightarrow$ 13.9 & 5 & 61.271 ms & 1 \\
        a3: Topology L2 & 10 & 23.6 $\rightarrow$ 15.7 & 23.5 $\rightarrow$ 13.9 & 5 & 59.477 ms & 1 \\
        a4: Topology L1+L2 & 10 & 23.6 $\rightarrow$ 15.7 & 23.5 $\rightarrow$ 13.9 & 5 & 67.305 ms & 1 \\
        \midrule
        a5: Latency L1 & 10 & 23.6 $\rightarrow$ 15.4 & 23.5 $\rightarrow$ 14.3 & 5 & 4.195 ms & 0 \\
        a6: Latency L2 & 10 & 23.6 $\rightarrow$ 15.4 & 23.5 $\rightarrow$ 14.3 & 5 & 3.017 ms & 0 \\
        a7: Latency L1+L2 & 10 & 23.6 $\rightarrow$ 15.4 & 23.5 $\rightarrow$ 14.3 & 5 & 2.649 ms & 0 \\
        \bottomrule
    \end{tabular}%
    }
    \caption{Descheduler Performance: LowNodeUtilization Scenario (MinBetterPercentage=75\%)}
    \label{tab:low_node_results}
\end{table}

As demonstrated in Table \ref{tab:low_node_results}, all evaluated strategies successfully reduced the CPU and Memory Standard Deviation across the cluster. This indicates that the core balancing objective of the descheduler was achieved, regardless of the filtering strategy applied.

\subsubsection{Resource Balance vs. Network Performance}
\label{subsubsec:netaware4_resultanalysis1_tradeoff}

While all strategies achieved equivalent resource balancing, their impact on network performance varied drastically. Figure \ref{fig:balance_vs_latency} illustrates this critical trade-off.
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \pgfplotsset{
            width=12cm,
            height=7cm,
            scale only axis,
            xmin=0.5, xmax=3.5,
            xtick={1,2,3},
            xticklabels={Baseline, Topology-Aware, Latency-Aware},
            enlarge x limits=0.2,
        }
        
        % Left Y-Axis: CPU & MEM Standard Deviation (Grouped Bar Chart)
        \begin{axis}[
            axis y line*=left,
            ymin=0, ymax=20,
            ylabel={Standard Deviation After Eviction (\%)},
            ybar=2pt,
            bar width=15pt,
            legend pos=north west,
        ]
            \addplot[fill=blue!60, draw=black] coordinates {
                (1, 15.7)
                (2, 15.7)
                (3, 15.4)
            };
            \addplot[fill=cyan!60, draw=black] coordinates {
                (1, 13.9)
                (2, 13.9)
                (3, 14.3)
            };
            \addlegendentry{CPU StdDev}
            \addlegendentry{MEM StdDev}
        \end{axis}
        % Right Y-Axis: Average Latency (Line Chart)
        \begin{axis}[
            axis y line*=right,
            axis x line=none,
            ymin=0, ymax=80,
            ylabel={Average Latency (ms)},
            legend pos=north east,
            mark=*,
            mark options={scale=1.5}
        ]
            \addplot[color=red, thick, mark=*] coordinates {
                (1, 61.717)
                (2, 67.305)
                (3, 2.649)
            };
            \addlegendentry{Latency (ms)}
        \end{axis}
    \end{tikzpicture}
    \caption{Cluster Balance vs. Network Performance. The Latency-Aware plugin achieves equivalent CPU and Memory balancing as the Baseline/Topology approaches, while completely preventing latency spikes.}
    \label{fig:balance_vs_latency}
\end{figure}

The baseline and topology-aware strategies successfully balanced the cluster but caused a catastrophic increase in average latency (spiking from $\sim$4ms to over 60ms). In contrast, the latency-aware strategies (a5-a7) achieved near equal cluster balance while maintaining the latency ($\sim$3ms).

The massive latency spikes observed in the topology-aware tests are primarily caused by the topology model's inability to detect actual network degradation. As visualized in Figure \ref{fig:topology_flaw}, the topology model only evaluates physical zone boundaries (e.g., Cross-Zone to Cross-Zone) and assumes all links are perfectly healthy, it considered degraded nodes as healthy one, resulting on lower or equal network cost calculation. The proposed latency-aware solved this issue by querying Prometheus for real-time metrics, correctly detecting the 160ms degradation and blocking the eviction.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            width=10cm,
            height=6cm,
            ymin=0, ymax=1.5,
            ylabel={Sensitive Pods Placed in Degraded Zone},
            symbolic x coords={Baseline, Topology-Aware, Latency-Aware},
            xtick=data,
            nodes near coords,
            every node near coord/.append style={yshift=5pt, font=\small},
        ]
            \addplot[fill=orange!70, draw=black] coordinates {
                (Baseline, 1)
                (Topology-Aware, 1)
                (Latency-Aware, 0)
            };
        \end{axis}
    \end{tikzpicture}
    \caption{Number of sensitive pods placed into a network-degraded zone. Topology-based filtering fails to detect actual latency, allowing a bad placement that ruins network performance.}
    \label{fig:topology_flaw}
\end{figure}

\subsubsection{The Limitations of Pre-Eviction Filtering (L1)}
\label{subsubsec:netaware4_resultanalysis1_limitation}

During testing, we also discovered that the global Pre-Eviction filter (L1) is highly sensitive to configuration heuristics. Table \ref{tab:minbetter_impact} compares the performance of the L1 filter under different \texttt{MinBetterPercentage} thresholds against the strategy-embedded L2 filter.

\begin{table}[H]
    \centering
    \caption{Impact of MinBetterPercentage on L1 Pre-Eviction Filters}
    \label{tab:minbetter_impact}
    \begin{tabular}{l c c}
        \toprule
        \textbf{Configuration} & \textbf{Degraded Pods} & \textbf{Avg Latency (ms)} \\
        \midrule
        L1 Pre-Eviction (MinBetter = 50\%) & 1 & 63.696 \\
        L1 Pre-Eviction (MinBetter = 75\%) & 0 & 4.195 \\
        \midrule
        L2 Strategy Filter (No Tuning Req.) & 0 & 3.017 \\
        \bottomrule
    \end{tabular}
\end{table}

When the L1 filter was configured with a loose threshold (50\%), it evaluated all schedulable nodes in the cluster and determined that 2 out of the 4 available candidate nodes offered equal or better latency. Because this satisfied the 50\% minimum threshold requirement, the L1 filter allowed the eviction. However, when the pod was rescheduled, the \textit{kube-scheduler} utilized its default \textit{LeastAllocated} strategy. Since the degraded node in the cluster was also completely empty, the scheduler simply placed the pod there to balance CPU and memory resources, completely destroying the workload's latency. 

To mitigate this global-filtering flaw, the L1 threshold had to be strictly tuned to 75\%. In contrast, the L2 filter embeds network-awareness directly into the destination-scoring logic. By evaluating only the exact target nodes rather than the entire cluster, L2 successfully filters out irrelevant "decoy" nodes and provides a much tighter safety net in this scenario, protecting the workload without requiring manual heuristic tuning.

%-----------------------------------------------------------------------------%
\subsection{Experimental Results: HighNodeUtilization Scenario}
\label{subsec:netaware4_resultanalysis2}
%-----------------------------------------------------------------------------%
\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{cllcccccc}
            \toprule
            \textbf{Scenario ID} & \textbf{Strategy Configuration} & \textbf{Metrics Provider} & \textbf{Eviction Count} (\(E_{count}\)) &\(\sigma_{cpu}\) & \(\sigma_{mem}\) &\(N_{\text{active}}\) & \(L_{\text{avg}}\) & \(S_{\text{degraded}}\) \\
            \midrule
                b1 & Upstream Baseline & None & 6 & 32.4\% & 35.0\% & 2 & 116.310ms & 3 pods \\
                b2 & Layer 1 Only & Topology & 4 & 29.4\% & 33.8\% & 3 & 82.055ms & 2 pods \\
                b3 & Layer 2 Only & Topology & 6 & 32.4\% & 35.0\% & 2 & 115.268ms & 3 pods \\
                b4 & Dual Layer (L1+L2) & Topology & 4 & 29.4\% & 33.8\% & 3 & 84.232ms & 2 pods \\
                b5 & Layer 1 Only & Latency & 4 & 29.4\% & 33.8\% & 3 & 86.119ms & 2 pods \\
                b6 & Layer 2 Only & Latency & 6 & 32.4\% & 35.0\% & 2 & 113.755ms & 3 pods \\
                b7 & Dual Layer (L1+L2) & Latency & 4 & 29.4\% & 33.8\% & 3 & 81.982ms & 2 pods \\
            \midrule
            \end{tabular}
    }
    \caption{Latency and Defragmentation Tradeoff: HighNodeUtilization Scenario Results (50\% \textit{MinBetterCandidatesPercent})}
    \label{tab:high_util_results1}
\end{table}

\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{cllcccccc}
            \toprule
            \textbf{Scenario ID} & \textbf{Strategy Configuration} & \textbf{Metrics Provider} & \textbf{Eviction Count} (\(E_{count}\)) &\(\sigma_{cpu}\) & \(\sigma_{mem}\) &\(N_{\text{active}}\) & \(L_{\text{avg}}\) & \(S_{\text{degraded}}\) \\
            \midrule
                b1 & Upstream Baseline & None & 6 & 32.4\% & 35.0\% & 2 & 114.919ms & 3 pods \\
                b2 & Layer 1 Only & Topology & 2 & 27.2\% & 32.9\% & 4 & 54.908ms & 1 pod \\
                b3 & Layer 2 Only & Topology & 6 & 32.4\% & 35.0\% & 2 & 117.951ms & 3 pods \\
                b4 & Dual Layer (L1+L2) & Topology & 2 & 27.2\% & 32.9\% & 4 & 51.184ms & 1 pod \\
                b5 & Layer 1 Only & Latency & 2 & 27.2\% & 32.9\% & 4 & 54.199ms & 1 pod \\
                b6 & Layer 2 Only & Latency & 6 & 32.4\% & 35.0\% & 2 & 119.465ms & 3 pods \\
                b7 & Dual Layer (L1+L2) & Latency & 2 & 27.2\% & 32.9\% & 4 & 52.910ms & 1 pod \\
            \midrule
            \end{tabular}
    }
    \caption{Latency and Defragmentation Tradeoff: HighNodeUtilization Scenario Results (75\% \textit{MinBetterCandidatesPercent})}
    \label{tab:high_util_results2}
\end{table}

The results across both threshold configurations reveal an important architectural trade-off between aggressive workload compaction and network locality preservation. In the un-aware baseline scenario (b1), the descheduler performs 6 pod evictions and successfully consolidates the workloads onto only 2 active nodes (\(N_{\text{active}}=2\)). This produces the highest resource density in the experiment, with 32.4\% and 35.0\%. However, because the balancing process has no visibility into network costs, the scheduler places 3 latency-sensitive \emph{checkout-flow} pods onto the logically degraded regional tier. As a result, the average cross-service latency increases dramatically to more than 114ms.

When we increase the \textit{MinBetterCandidatesPercent} threshold from 50\% to 75\ on Layer 1 pre-eviction filter, itrespond more conservatively. Under the 50\% threshold, the descheduler performs 4 evictions, whereas under the stricter 75\% threshold it reduces the number of evictions to only 2. This happens because the stronger filtering rules prevent the descheduler from evicting \emph{checkout-flow} pods unless it can verify that at least 75\% of the available destinations satisfy the network locality requirements. As a consequence, cross-region spill is reduced from 2 pods to only 1 pod, lowering the average cross-service latency from approximately 82ms to around 51ms. The trade-off is a slightly less compact cluster state, where 4 active nodes remain in use instead of 3.

A major anomaly appears in the Layer 2 target-selection configurations (b3 and b6). In both threshold configurations, these scenarios fail to preserve network locality and instead behave almost identically to the un-aware baseline. Both scenarios perform 6 evictions, reduce the infrastructure footprint to 2 active nodes, spill 3 latency-sensitive pods across regions, and produce severe latency penalties between approximately 115ms and 119ms.

From the scheduling logs, we observe that this behavior is caused by a desynchronization loop between the descheduler’s target-selection logic and the default Kubernetes scheduler.

\begin{itemize}
    \item \textbf{Initial Cluster State:}  
    During the initial workload placement, the most heavily utilized nodes are worker-2 (high-performance tier) and worker-5 (network-degraded tier), while others three worker remains relatively underutilized.

    \item \textbf{Descheduler Evaluation Phase:}  
    The Layer 2 strategy identifies worker-3 as underutilized and selects its \emph{checkout-flow} pods as eviction candidates. During destination filtering, the network cost provider evaluates the available nodes and correctly identifies worker-2 as low-latency, high-performance targets. Since acceptable destinations exist within the same region, the Layer 2 policy which only expect one better candidates approves the eviction with the expectation that the pods will relocate to those nodes.

    \item \textbf{Scheduler Execution Phase:}  
    After the descheduler issues the eviction request, the pods return to the default Kubernetes scheduler queue. At this stage, the upstream scheduler still operates using a network-unaware \emph{MostAllocated} scoring strategy that prioritizes packing density over communication locality.

    \item \textbf{Scheduling Race Condition:}  
    Because earlier evictions in the same cycle have already moved additional workloads onto worker-2, its allocatable resources become saturated before the next pod is scheduled. When the scheduler evaluates the newly evicted pod, worker-2 is no longer available. The scheduler then selects worker-5 because it is the next most heavily utilized node that still satisfies the resource constraints.
\end{itemize}

Since the default Kubernetes scheduler has no understanding of network costs, we found it unintentionally overrides the descheduler’s locality-aware placement intentions and relocates the pod onto the degraded regional tier. This demonstrates a fundamental limitation of descheduling only approaches as for some scenario the descheduler target-awareness alone is insufficient if the underlying scheduler does not enforce the same network-aware constraints. Without a network-aware scheduler controlling the final landing decisions, the descheduler and scheduler can become desynchronized, consequently leading to unintended pods reschedule.

%-----------------------------------------------------------------------------%
\subsection{Experimental Results: ImbalanceIndexUtilization Scenario}
\label{subsec:netaware4_resultanalysis3}
%-----------------------------------------------------------------------------%
\begin{table}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{cllcccccc}
            \toprule
            \textbf{Scenario ID} & \textbf{Strategy Configuration} & \textbf{Metrics Provider} & \textbf{Eviction Count} (\(E_{count}\)) &\(\sigma_{cpu}\) & \(\sigma_{mem}\) &\(N_{\text{active}}\) & \(L_{\text{avg}}\) & \(S_{\text{degraded}}\) \\
            \midrule
                c1 & Upstream Baseline & None & 2 & 19.1\% & 26.4\% & 3 & 98.370ms & 1 pod \\
                c2 & Layer 1 Only & Topology & 1 & 25.8\% & 28.6\% & 2 & 6.138ms & 0 pods \\
                c3 & Layer 1 Only & Latency & 1 & 25.8\% & 28.6\% & 2 & 6.138ms & 0 pods \\
            \midrule
            \end{tabular}
    }
    \caption{Latency and Defragmentation Tradeoff: ImbalanceIndexUtilization Scenario Results (75\% \textit{MinBetterCandidatesPercent})}
    \label{tab:imbalance_results}
\end{table}

The empirical results from Session 3 highlight how volatile multi-dimensional resource optimization can become when network locality constraints are introduced into the balancing process. In the initial cluster state generated by \textit{reset-imbalance.sh}, worker-585 is intentionally configured to be CPU-heavy, carrying approximately 60\% CPU allocation and 30\% memory allocation, while the simulated degraded node worker-930 becomes memory-heavy with only 2.5\% CPU allocation but 61\% memory allocation. This creates a highly asymmetric workload distribution where exchanging pods between nodes would mathematically improve the overall balancing profile of the cluster.

In the un-aware baseline scenario (c1), the TOPSIS-based balancing strategy performs 2 pod evictions in an attempt to reduce this imbalance. Since the plugin evaluates nodes purely from a resource optimization perspective, it identifies that redistributing the skewed workloads would improve the cluster's multi-dimensional utilization score. However, because the strategy has no understanding of network locality, one latency-sensitive \emph{checkout-flow} pod is relocated across the degraded logical network boundary onto worker-930. This immediately creates a cross-region spill condition (\(S_{\text{degraded}} = 1\)) and causes the average cross-service latency to rise sharply to 98.370ms.

When we enable the network-aware Layer 1 pre-eviction filtering mechanism (c2 and c3), we observe a much more controlled balancing behavior. Before the descheduler finalizes an eviction, the \textit{NetworkCostEvictor} evaluates the communication penalty that would occur if the targeted \emph{checkout-flow} pod were separated from its related service dependencies across the degraded network path.

Once the calculated network penalty exceeds the configured policy threshold, the filter blocks the migration. As a result, the total eviction count decreases from 2 to 1, successfully preventing the latency-sensitive workload from being moved into the degraded region. This keeps the cross-region spill count at exactly 0 pods and preserves a low average cross-service latency of only 6.138ms.

Although this protection mechanism slightly reduces the efficiency of the resource optimizer’s balancing behavior, it helps preserve the bin-packing principle of scheduling pods onto nodes. As a result, the system maintains a controlled distribution, with variance reaching 25.8\% for CPU and 28.6\% for memory while operating with only two active nodes.
